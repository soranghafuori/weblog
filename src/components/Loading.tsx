import React from 'react';
import ReactLoading from 'react-loading';
 
function Loading() {
    return ( <ReactLoading type="bubbles" color="black" height={267} width={175} /> );
}

export default Loading;